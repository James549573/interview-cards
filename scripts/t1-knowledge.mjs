// ---------- T1 卡人工核校内容（展示层重构 V6） ----------
// 原则：卡片是给用户看的。curated 内容只写知识（它是什么 / 为什么这么做 / 面试会怎么问），
// 审核证据（定位说明、来源等级、互证匹配）一律不进卡片，迁 TRACE_REPORT / R21_SOURCE_GRADES。
// 未覆盖的 T1 卡走 parseT1Index 自动组装（挂钩句 + 关联卡摘录）；自动内容不足时
// 只保留"它是什么"段，不用上下文推断填满四段（硬约束，2026-09-25 大山哥定）。

export const T1_CONTENT = {
  'T1-32': {
    what:
      '网关对每次调用**先同步校验配额**——网关是唯一能在调用发生前拦住花销的位置，预算超了直接拒绝，防止超支的调用被放行。**审核和计量异步执行**——不阻塞主链路，调用先返回，审核计量后台补做。整条链路的性能上限：网关额外时延 **P99 ≤ 30ms**。**fail-closed**：配额服务不可用时拒绝调用，而不是跳过校验放行。',
    why: [
      '- **配额前置**：预算超支不可挽回，只能在调用前拦——事后追责追不回已经花掉的钱。',
      '- **审核异步**：主链路低延迟。审核本身按开销分层（关键词 <5ms → 分类模型 <50ms → 只有大模型复核 ≤10% 边界样本），异步设计才扛得住这个链路。',
      '- **fail-closed**：服务故障时不超支，宁可拒绝也不能放行。'
    ].join('\n'),
    questions: [
      '为什么审核可以异步，配额必须同步？（一个事后可补，一个事前不可挽回——K-21）',
      '配额服务挂了为什么不能 fail-open 放行？（网关是调用前唯一能拦花销的位置）',
      'P99 ≤ 30ms 为什么用 P99 不用平均值？',
      '异步审核出问题时怎么追溯？（审核留痕 + 计量数据不可删）'
    ],
    sourceNote: 'Prompt 基础设施线核心 · 第 2 条；机制展开取自 K-21 正文与同线审核三级串联句（I5）'
  },

  'T1-24': {
    what:
      '向量库不押注单一方案：**规模化集中部署用 Milvus**（独立向量库集群，支持横向扩展与大规模检索），**轻量 / 单机部署用 pgvector**（Postgres 扩展，与业务数据同库同事务，运维成本最低）。架构登记为"双选"，禁止把检索层写死在其中一个上。',
    why:
      '素材只写"双选禁止单一方案"，未展开原因。以下为上下文推断：政务项目的部署环境在立项时往往未最终确定（省级集中 vs 区县独立部署）。押单一方案，等于把检索层绑死在一种运维形态上；双选的本质是**环境适配性决策**，不是性能选型——性能上两者都够用，环境不一定。',
    questions: [
      '两个都上吗？运行时到底用哪个？（登记双选是保留适配余地，实际按部署环境二选一；检索层做抽象接口隔离向量库实现——此为工程推断，面试时主动声明）',
      '什么数据量级必须用 Milvus、pgvector 就够？',
      '换部署环境时检索层要改哪些东西？'
    ],
    sourceNote: 'Prompt RAG 全流程线核心 · 第 6 条；理由与切换思路为上下文推断（已在正文声明）'
  },

  'T1-30': {
    what: [
      'RAG 线的合规红线汇总：12 条明令禁止项——',
      '',
      '1. **越权召回 0 次**——一票否决项，写死 0，不写 ≤1%',
      '2. **no_hit 一律强制拒答**——检索无命中不自由生成',
      '3. **三路检索禁止分数融合**——三路分数不可比，先合并去重再统一重排',
      '4. **禁止单一向量库方案**——Milvus + pgvector 双选，防环境锁死',
      '5. **重排必须本地化**——BGE-reranker 本地部署，禁用外部重排 API',
      '6. **缓存键必须含 scope**——禁止跨租户命中缓存',
      '7. **查询侧原文不落库**——用 query_hash 替代原文存储',
      '8. **引用与审计 fail-closed**——审计写入失败即阻断调用，不能带病返回',
      '9. **上下文组装必须二次复核**——组装进提示词的内容要复核权限与口径',
      '10. **top_k 必须落在对象矩阵登记区间**——不得随意调大检索范围',
      '11. **AI-602 三键锁定是过滤不是检索**——三键不得计入检索路数，混谈会导致 top_k 语义混乱',
      '12. **图谱不与向量做分数融合、构建失败不阻断向量检索**——图谱列二期，MVP 只上向量 + 关键词两路'
    ].join('\n'),
    why: '这 12 条全部是"写死在验收口径里"的硬约束——违反任何一条都不是扣分项，是整条 RAG 链路论证作废。',
    questions: [
      '12 条里哪条是一票否决？（越权召回 0 次）',
      '为什么禁止分数融合？（三路量纲不可比——K-17）',
      'AI-602 三键锁定算不算第三路检索？（不算，是过滤——K-17）',
      '审计为什么必须 fail-closed？（证据问题不是质量问题——K-21）'
    ],
    sourceNote: 'Prompt RAG 全流程线核心 · 第 21 条；逐条来源等级见项目根 R21_SOURCE_GRADES.md'
  }
};

// ---------- A 档 T1 卡（V7 分档制）：挂钩句含多面信息，四段式手写 ----------
// 分档标准（2026-09-25 大山哥判定）：A=挂钩句有多面信息（机制/枚举/参数），能诚实写满四段；
// B=挂钩句只有一句陈述，只写"它是什么"+关联指引。B 档卡在 parseT1Index 自动生成。
// relatedOverride：人工核校的关联卡（替换 hookMap/bodyMention 自动映射）——
// 只列与 T1 主题直接相关的卡，禁止贴不相关段落（T1-11 教训）。
export const T1_A = {
  'T1-09': {
    what:
      '问答入口不是随系统一起上线的：**语义层先行**——先把指标口径层建起来，首批 **≥50 个指标且全部已审批**（G9 门槛）才开放自然语言问答入口。语义层没覆盖的问题一律 `match_type=UNSUPPORTED` 明确回"暂不支持"，并把问题记录为指标需求进甲方评审队列。',
    why: [
      '- **先内容后入口**：入口先开、内容后补，用户头几次提问就答不上，信任一次性丢掉——语义层先行的本质是"能答的先答准"。',
      '- **为什么是 50 且要已审批**：50 是覆盖常用提问的最小集（素材口径）；"全部已审批"让口径责任在甲方——入口开放的每一天，里面的答案都是甲方认过的。'
    ].join('\n'),
    questions: [
      '为什么是 50 个，不是 20 或 100？（首批覆盖常用指标的最小集，素材口径）',
      '用户问了语义层没有的指标怎么办？（UNSUPPORTED 拒答 + 记指标需求——K-01/K-06）',
      '为什么要求"全部已审批"才开入口？（口径责任归甲方——G9 门槛）'
    ],
    relatedOverride: ['K-27', 'K-04', 'K-01'],
    sourceNote: 'Prompt AI-410 数据问答线核心 · 第 11 条；G9 门槛与 UNSUPPORTED 机制见 K-01/K-04/K-27'
  },

  'T1-10': {
    what:
      '评测集的正样（标准答案集）由**甲方主导编写**；承建方不碰正样，只负责造**对抗样本**（专门攻击系统弱点的边界问法）。',
    why: [
      '- **裁判不能是运动员**：评测集承建方自己造自己考，调优即刷分（RK-08 登记风险），分数好看但不能证明达标。',
      '- **对抗样本反而可以交给承建方**：对抗样本帮系统在交付前暴露弱点，造得越狠对甲方越有利——利益方向和正样正好相反。'
    ].join('\n'),
    questions: [
      '为什么正样不能承建方写、对抗样本可以？',
      '对抗样本和正样的比例怎么定？',
      '甲方没有人力写评测怎么办？（与 K-18 留出集隔离怎么配合）'
    ],
    relatedOverride: ['K-18'],
    sourceNote: 'Prompt AI-410 数据问答线核心 · 第 21 条；调优即刷分风险见 K-18'
  },

  'T1-11': {
    what: [
      '上线前设 10 道硬门槛（G1–G10），**任何一道不满足就整体不上线，无例外**。素材披露的门槛包括：',
      '',
      '- **越权 / 注入 / 写操作 = 0、个体行 = 0、审计覆盖 100%、拒答正确率 100%**（K-19 披露句）',
      '- **G1**：「AI 结论 = 0」不给结论——一票否决，且不允许整改后复验',
      '- **G9**：首批 ≥50 个指标全部已审批才开问答入口'
    ].join('\n'),
    why: [
      '- **门槛让"能不能上"变成客观判断**：不满足就停，避免带着已知缺陷上线——返工和事故的成本都比延期高。',
      '- **为什么不给例外**：否决项（如给过一次 AI 结论）一旦发生无法撤销，G1 类错误不存在"整改后复验"，只能前置拦住。'
    ].join('\n'),
    questions: [
      'G1–G10 分别考核什么？（素材只披露上列几项，答不全要直说）',
      '为什么否决项不允许整改后复验？',
      '硬门槛和验收指标有什么区别？（验收可分期达标，门槛是 0/1 判断）'
    ],
    relatedOverride: ['K-19', 'K-25', 'K-33'],
    sourceNote: 'Prompt AI-410 数据问答线核心 · 第 19 条；门槛披露句见 K-19，G1 论证见 K-33，G9 见 K-01/K-04'
  },

  'T1-15': {
    what:
      '业务规则不写死在代码里：规则用 **DSL**（规则描述语言）单独表达，由统一的**求值器**执行；规则改版时**新旧版本并存**，可回退可对照。',
    why: [
      '- **改规则不发版**：规则（门槛值、比例、清单）的变更频率远高于代码，写死在代码里每次改都要走发版回归。',
      '- **可审计、可回退**：规则独立成文本才谈得上 diff 和评审；版本并存保证新版出错时一条配置切回旧版，而不是紧急发版。'
    ].join('\n'),
    questions: [
      'DSL 表达力不够时怎么办？（什么该进 DSL、什么该留在代码）',
      '求值器本身出 bug 影响面多大，怎么兜底？',
      '版本并存时以哪个版本为准？（生效时间与切换怎么管）'
    ],
    relatedOverride: [],
    sourceNote: 'Prompt AI-401 招生核验线核心 · 第 9 条'
  },

  'T1-16': {
    what:
      '规则判定结果分**确定**与**存疑**两态；存疑不是降级通过，而是**硬阻塞**——决策接口 `/decision` 直接返回 **409**，不给结论。与 K-09 三值规则同源：查不到落 UNDETERMINED，绝不落 NOT_SATISFIED。',
    why: [
      '- **存疑结果不能当结论用**：引擎自己都不确定时给出判定，等于把不确定性转嫁给下游，且下游无从分辨。',
      '- **为什么用 409 而不是 200+warning**：状态码层面就让调用方知道"这条没有判定结果"，必须人工介入；warning 字段太容易被忽略。'
    ].join('\n'),
    questions: [
      '为什么 409 不是 200 加 warning 字段？',
      '存疑状态怎么解除？（人工复核流程怎么接）',
      '存疑率过高会不会堵死正常流量？（存疑率怎么监控、阈值谁定）'
    ],
    relatedOverride: ['K-09'],
    sourceNote: 'Prompt AI-401 招生核验线核心 · 第 5 条；三值规则见 K-09'
  },

  'T1-19': {
    what:
      '运维侧六条红线（V18）。素材点名的一例：结构性禁止项（如 `has_qualification_conclusion` 恒为 FALSE）的关闭与修改必须走审批（K-08 引用为六条红线之一），其余条目素材未逐条披露。配套排期原则：**"能力可后置，约束不可后置"**。',
    why: [
      '- **能力后置影响功能完整度，约束后置意味着系统在裸奔期运行**：少一个功能可以补，少一道权限/审计约束出的事故不可逆。',
      '- **红线是"关闭需审批"而不是"永久不可动"**：业务会变，但变更必须走评审留痕，不能运维随手改配置。'
    ].join('\n'),
    questions: [
      '六条运维红线分别是哪些？（素材只点名个别条目，答不全要直说）',
      '判断"能力 vs 约束"的标准是什么？',
      '为什么约束的变更要走评审留痕？'
    ],
    relatedOverride: ['K-08', 'K-34'],
    sourceNote: 'Prompt AI-401 招生核验线核心 · 第 18 条；红线应用例见 K-08，红线检查表见 K-34'
  },

  'T1-21': {
    what:
      'AI-401 风险计提两条口径：**主体不计提**；**组合形态按子项计 30%**。与 K-23 的"30% / 50% 两档、不叠加"规则同源。',
    why: [
      '- **主体不计提**：主体本身就是业务本体，再对它计提等于重复算风险。',
      '- **组合按子项计提**：打包形态的风险来自子项，逐项计提才对得上风险来源；按包总价一刀切会放大或缩小真实风险。'
    ].join('\n'),
    questions: [
      '为什么主体不计提，什么算"主体"？',
      '30% 和 50% 两档怎么划分？（K-23：按形态不按风险等级）',
      '两档为什么不叠加？'
    ],
    relatedOverride: ['K-23', 'K-34'],
    sourceNote: 'Prompt AI-401 招生核验线核心 · 第 15 条；两档判据见 K-23'
  },

  'T1-22': {
    what:
      '文档切分按**层级**进行，每个切分块保留两个定位字段：**chapter_path**（章节路径）与 **page_no**（页码）。',
    why: [
      '- **生成答案要带引用**：只存正文不存位置，答案只能说"文档里提到了"，说不出在第几章第几页——甲方无法核对，用户无从信任。',
      '- **两个字段各管一头**：chapter_path 定位逻辑结构（语义位置），page_no 定位物理位置（原件核对），丢一个引用链就断。'
    ].join('\n'),
    questions: [
      '为什么 chapter_path 和 page_no 都要，只有一个不行吗？',
      '层级切分的边界怎么定（按标题还是按语义）？',
      '引用定位错了怎么验收？（评测错误七分类含切分——K-18）'
    ],
    relatedOverride: ['K-17', 'K-18'],
    sourceNote: 'Prompt RAG 全流程线核心 · 第 3 条；评测七分类见 K-18'
  },

  'T1-23': {
    what:
      '页码定位规则**按知识库类型（kb_type）分档**——手册、合同、问答对的"页"语义不同，不一刀切；溯源级别 **provenance_level 设三级**，允许从精确引用逐级降级。',
    why: [
      '- **分档**：统一页码语义会在结构差异大的文档上错位，分档是承认"页"在不同载体上含义不同。',
      '- **三级降级**：不是每次都能定位到页——给出诚实的降级路径，比硬凑一个可能错的引用好；降级必须留痕、不展示缓存结果（K-05）。'
    ].join('\n'),
    questions: [
      'kb_type 有哪几类，页码规则分别怎么定？',
      '三级降级每级的判定标准和呈现方式？',
      '降到最低级时答案还敢给吗？（L3 不自由生成——R16）'
    ],
    relatedOverride: ['K-05', 'K-01', 'K-02'],
    sourceNote: 'Prompt RAG 全流程线核心 · 第 4 条；降级纪律见 K-05'
  },

  'T1-25': {
    what:
      '重排（rerank）模型用**本地部署**的 BGE-reranker-v2-m3（Top50 → Top5），**禁用商用 / 外部重排 API**。',
    why: [
      '- **重排要读全文**：query 和候选片段全文都要过重排模型——调外部 API 等于把语料送出域，数据红线直接失守。',
      '- **本地化的代价是自运维**：延迟、显存、模型版本都要自己扛，但在政务场景这笔账没有第二种算法。'
    ].join('\n'),
    questions: [
      '为什么重排禁外部 API，向量库却允许双选？（重排接触全文，风险量级不同）',
      'Top50 → Top5 的档位怎么定的？',
      '本地重排延迟不达标怎么优化？'
    ],
    relatedOverride: ['K-17'],
    sourceNote: 'Prompt RAG 全流程线核心 · 第 7 条；模型与档位原文见 K-17'
  },

  'T1-26': {
    what:
      '检索 / 答案缓存的键**必须包含 scope（租户与权限域）**；任何跨租户的缓存命中都是违规，不是"尽量避免"。',
    why: [
      '- **越权不需要攻击，一次巧合就够**：键里没有 scope，A 租户的高频问题可能直接命中 B 租户缓存过的答案。',
      '- **写进键 = 结构上不可能**：scope 进键后跨租户命中在机制上被排除，不依赖调用方自觉——与权限谓词前置（K-03 scope_resolver）是同一条纪律。'
    ].join('\n'),
    questions: [
      'scope 除了租户还包含什么？（角色、数据域怎么进键）',
      '缓存在权限过滤之前还是之后？',
      '缓存更新与失效策略怎么保证租户间互不影响？'
    ],
    relatedOverride: ['K-03', 'K-05'],
    sourceNote: 'Prompt RAG 全流程线核心 · 第 8 条；scope 纪律见 K-03，缓存降级见 K-05'
  },

  'T1-27': {
    what:
      '语料授权的四项允许用途，**落成数据库 CHECK 约束**（落点：招生资格材料抽取表）——不是写在文档里，而是写进表结构，超范围用途在数据层直接被拒。K-08 称其为"全项目合规如何落地的最佳例证"。',
    why: [
      '- **授权范围写成文档，绕过只需要一句"应该没问题"**；落成 CHECK，超范围操作在数据库层直接报错。',
      '- **把合规从"靠自觉"变成"靠结构"**：与"验收证据是结构不变量"（K-22）同一治理逻辑。'
    ].join('\n'),
    questions: [
      '四项用途分别是什么？（素材口径，需能逐条报出）',
      '为什么用 DB CHECK 而不是应用层校验？',
      '要新增用途时流程怎么走？（先改授权还是先改约束）'
    ],
    relatedOverride: ['K-08', 'K-21'],
    sourceNote: 'Prompt RAG 全流程线核心 · 第 9 条；CHECK 落点见 K-08'
  },

  'T1-29': {
    what:
      '检索失灵时按三级降级：**L1 关键词兜底 → L2 缓存命中 → L3 直接拒答**；到 L3 **绝不自由生成**。',
    why: [
      '- **三级按可信度递减排的**：关键词结果可溯源，缓存答案曾经被验证过，自由生成两者都不具备。',
      '- **降级的意义是诚实**：宁可说"现在答不了"，也不能给一条没有出处的话；降级必须留痕、不展示缓存结果（K-05）。'
    ].join('\n'),
    questions: [
      '三级的触发顺序和判定条件？',
      'L2 缓存命中怎么保证仍然有效（权限、时效）？',
      '为什么 L3 宁可拒答也不让模型兜底？（与 no_hit 强制拒答——R2 同源）'
    ],
    relatedOverride: ['K-25', 'K-05'],
    sourceNote: 'Prompt RAG 全流程线核心 · 第 16 条；降级留痕与缓存纪律见 K-05'
  },

  'T1-34': {
    what:
      '数据侧三条不可协商的红线：① **原文不落库**（原始语料原文不进应用库表）；② **留存有期 ≥6 个月**；③ **身份可去**（支持去标识）。',
    why: [
      '- **原文不落库**：库表里留的是结构化结果与 hash，泄露面从"整个语料"缩到"索引与结论"。',
      '- **留存有期 ≥6 个月**：审计追溯需要时间窗——跨季度的争议、申诉复盘都依赖这个窗口。',
      '- **身份可去**：去标识必须是可执行的技术动作，不能只是合同承诺。'
    ].join('\n'),
    questions: [
      '原文不落库，检索和审计怎么做？（结构化结果 + hash 替代）',
      '6 个月的依据是什么？（审计与申诉复盘的时间窗）',
      '"身份可去"在技术上落地是什么操作？'
    ],
    relatedOverride: [],
    sourceNote: 'Prompt 基础设施线核心 · 第 4 条'
  },

  'T1-35': {
    what:
      'AIGC 内容做**双标识**：**显式标识**（人可直接看到的生成声明）+ **隐式标识**（嵌在内容里、肉眼不可见但机器可读的标记，如水印 / 元数据）。',
    why: [
      '- **显式标识管告知**：让看的人知道这是 AI 生成，满足披露义务。',
      '- **隐式标识管溯源**：内容被截图、转发、抹掉显式声明后，仍能机器验出来源——只做显式，标识随转发就丢了；只做隐式，普通人不知道是 AI 内容。两条一起才闭环。'
    ].join('\n'),
    questions: [
      '显式和隐式标识分别防什么？',
      '隐式标识被恶意抹除怎么办？',
      '标识由生成侧打还是平台侧打？'
    ],
    relatedOverride: [],
    sourceNote: 'Prompt 基础设施线核心 · 第 7 条'
  },

  'T1-36': {
    what:
      '系统对未成年人敏感自述只做两步：**识别**出来、**转介**给该接手的人和流程；明确**不做诊断、不做干预**。',
    why: [
      '- **这条线划在能力边界上**：AI 没有临床与干预资质，给出"干预建议"就越界了；识别 + 转介把风险交给有资质的环节，系统只承担"发现"。',
      '- **边界写成否定句**："不诊断不干预"是否定式约束，可验收、可追责——与第三方边界合同化（M1）同一手法。'
    ].join('\n'),
    questions: [
      '什么算"敏感自述"，识别怎么做（关键词 / 模型 / 阈值）？',
      '转介给谁、多长时间内必须接手？',
      '为什么连"建议"都不能给？'
    ],
    relatedOverride: [],
    sourceNote: 'Prompt 基础设施线核心 · 第 8 条'
  },

  'T1-39': {
    what: [
      '第三方的"不参与"边界写成**合同否定句**，并落到具体条款：',
      '',
      '- **CL-13**：人工评审不得由承建方自查',
      '- **CL-18**：独立复算不得由乙方或承建方承担'
    ].join('\n'),
    why: [
      '- **正面表述留空子**："甲方负责评审"没禁止承建方参与——合同没禁的事都可争；否定句把禁区写成义务，违约可判定。',
      '- **落到 CL 编号**：每条边界可引用、可审计，出事时能对到具体条款。'
    ].join('\n'),
    questions: [
      '为什么必须否定句？（正面表述漏掉了什么）',
      '独立复算该由谁做才合规？',
      '发现承建方自查违反 CL-13 怎么处置？'
    ],
    relatedOverride: ['K-25', 'K-03'],
    sourceNote: 'Prompt 管理 / 交付线核心 · 第 1 条；条款原文即挂钩句'
  }
};
