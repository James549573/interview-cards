# V4 残余问题逐条验证报告（VERIFY_REPORT_V4）

生成：2026-09-25
原则：**只做验证，不做新修复**。本轮所有判定基于素材原文逐字核对与 git 提交快照（V2 = commit `5acc1b5`，V3 = `0d5ed62`），取证脚本可复跑：`scripts/_verify-v4.mjs`、`scripts/_verify-v4b.mjs`，输出快照 `_verify-v4-output.txt`、`_codes-v2.json`。

## 〇、判定总览

| 验证 | 判定 | 触发修复 |
|---|---|---|
| V-01 R21 溯源 | **通过（有修正）**：12 条全部可溯源，但来源等级实为 **8 A + 4 A′**，V3 文字"7+5"数错（表格本身是对的） | F-01 不触发 |
| V-02 40→22 去向 | **伪问题**：V2 的"约 40"是估数，V2 快照实测纯家族 22 条，与 V3 的 22 完全一致，**0 条消失** | F-02 改判：需修的不是"去向"，是下述 name 覆盖 bug |
| V-03 M8 展示 | **不通过**：点开 M8 是"27 句主题池"，看完说不出 M8 是什么 | **F-03 触发** |
| V-04 M27 逻辑 | **不矛盾，修复应保留**：编号来自素材挂钩标注而非位置对位。但发现**新执行 bug**：M27/M35/M21 的 name 字段未随 V3 修正更新 | **F-04 改判：修 name 覆盖 bug** |
| V-05 K-29 对照 | **判定成立**：素材原文 ≈398 字（含空白 397），四段逐字全在卡中，无漏读 | F-05 不触发 |
| V-06 G 系列穷举 | **V3 口径部分不成立**：G-1 在素材中有明确定义「AI 结论 = 0」，当前 codes.json 仍标"素材未逐条展开" | **F-06 触发（G1，G4 部分）** |

---

## 一、V-01：R21 溯源表逐条证据

### 1.1 A 与 A′ 的明确定义

- **A** = 该条内容在 Prompt「RAG 全流程线（R1–R21）核心」的 21 句分号枚举中有**直接对应句**（素材原句）。
- **A′** = 该条内容在素材**其他段落**（K-17/K-19 正文与追问答）有原文依据，但 21 句枚举中无对应句。
- **A′ 为什么能算 R21 的内容**：素材依据不只是枚举——**K-17 的卡标题原文即写着【已挂钩 R5 / R3 / R21】**（合并版 L603），R21 是 K-17 的挂钩编号之一，K-17 正文就是 R21 的素材展开位置之一；R21 本身只是"12 条明令禁止项"的汇总编号，素材没有提供 12 条的独立清单，因此 12 条 = 8 条枚举句 + 4 条 K 卡正文，全部是素材原文，无模型自带知识。

### 1.2 12 条逐条证据表

| # | 12 条内容 | 来源类型 | 源句原文（完整引用） | 素材文件 | 素材章节 |
|---|---|---|---|---|---|
| 1 | 越权召回 0 次，写死不写 ≤1% | **A′** | "`AC-03 越权召回 0 次`、`AC-06 缺陷处置率 100%`、`AC-07 授权挂接率 100%`——**写死**，不写"≤1%"或"≥99%"。" | 面试官证据源包_T0核心合并版.md:665（T0_模块3 同文:95） | K-19 追问答 |
| 2 | no_hit 一律强制拒答 | **A** | "no_hit 一律强制拒答" | 面试官Prompt_可直接复制.md:80 | R 线核心·句 2 |
| 3 | 三路检索禁止分数融合 | **A** | "三路混合检索"先合并去重再统一重排"（图谱列二期）" | 面试官Prompt:80 | R 线核心·句 5（K-17 标题同句：L603） |
| 4 | 禁止单一向量库方案 | **A** | "向量库 Milvus + pgvector 双选禁止单一方案" | 面试官Prompt:80 | R 线核心·句 6（T1-24 卡名同句互证） |
| 5 | 重排本地化，禁外部 API | **A** | "重排本地化 BGE-reranker 禁用外部 API" | 面试官Prompt:80 | R 线核心·句 7（K-17 正文展开） |
| 6 | 缓存键必须含 scope | **A** | "缓存键含 scope 禁止跨租户" | 面试官Prompt:80 | R 线核心·句 8（T1-26 卡名同句互证） |
| 7 | 查询原文不落库用 query_hash | **A** | "查询侧原文不落库用 query_hash" | 面试官Prompt:80 | R 线核心·句 13 |
| 8 | 引用与审计 fail-closed | **A** | "引用与审计 fail-closed"；"审计写入失败即阻断调用（fail-closed，R12/V-33）" | 面试官Prompt:80；合并版:674 | R 线核心·句 12 + K-19 追问答 |
| 9 | 上下文组装二次复核 | **A** | "上下文组装二次复核" | 面试官Prompt:80 | R 线核心·句 14（K-18 标题挂钩 R14） |
| 10 | top_k 落在对象矩阵登记区间 | **A′** | "③ `top_k` 须落在对象矩阵登记区间" | 合并版:609 | K-17 正文 |
| 11 | AI-602 三键锁定是过滤不是检索 | **A′** | "④ AI-602 的"三键锁定"（county_code + apply_year + 学段）是**过滤条件不是第三路**（ISSUE-151）。" | 合并版:609 | K-17 正文 |
| 12 | 图谱不与向量融合、构建失败不阻断 | **A′** | "① 图谱（NebulaGraph）引入但列为**二期**，MVP 先上向量 + 关键词两路；② 图谱构建失败不阻断向量检索" | 合并版:609 | K-17 正文 |

**等级统计修正**：实际 **A = 8 条（#2~#9）、A′ = 4 条（#1、#10、#11、#12）**。V3 报告（TRACE_REPORT L28 与 FIX_REPORT_V3）写的"7 条枚举原句、5 条 K 卡正文"**数错了**——TRACE 表格本身的行标注就是 8 A + 4 A′。属文字口径错误，不影响"全部可溯源、C 类 0"的结论。**F-01 不触发重构。**

### 1.3 21 句枚举全文（Prompt L80，编号 1~21）

1. 权限过滤定检索层（第 8 层，谓词由 ctx_ref 派生）
2. no_hit 一律强制拒答
3. 层级切分保留 chapter_path + page_no
4. page_no 按 kb_type 分档 + provenance_level 三级降级
5. 三路混合检索"先合并去重再统一重排"（图谱列二期）
6. 向量库 Milvus + pgvector 双选禁止单一方案
7. 重排本地化 BGE-reranker 禁用外部 API
8. 缓存键含 scope 禁止跨租户
9. 语料授权四项用途落成 CHECK
10. 新增 6 张承载表关键属性显式列
11. 引用可溯性分母书面界定
12. 引用与审计 fail-closed
13. 查询侧原文不落库用 query_hash
14. 上下文组装二次复核
15. 索引版本原子切换 + 换版三段式
16. 三级降级（L1 关键词 / L2 缓存 / L3 拒答，L3 不自由生成）
17. 评测甲方主导 + 错误七分类
18. 26 对象四级裁决
19. 工时单一权威口径
20. 口径冲突不抹平分库解决
21. **12 条明令禁止项** ← R21

**对位可信度（本轮复核）**：句 6 = T1-24 卡名"向量库 Milvus + pgvector 双选禁止单一方案"、句 7 = T1-25"重排本地化 BGE，禁用外部 API"、句 8 = T1-26"缓存键含 scope，禁止跨租户缓存"、句 9 = T1-27"语料授权四项用途落成 CHECK"（cards.json 实测）、句 12 = K-19 正文"fail-closed，R12/V-33"（合并版:674）、句 17 = K-18 主题（标题挂钩 R17）、句 14 = K-18 标题挂钩 R14。枚举末句即 R21，**21 句枚举不在别处，就是 R 线"核心"行本身**。

### 1.4 与 V2 判定"素材未逐条列出"的冲突解释

V2 版 R21（commit 5acc1b5 快照实测）头声明为"素材未逐条列出 12 条原文，以下是按素材 RAG 线已写明的禁止性约束逐条归纳"，12 条与 V3 版**逐条同内容、同出处**。两者不真矛盾：

- V2 说的是"素材没有以『禁止项 1/2/3』形式给出 12 条独立清单"——**字面为真**，本轮再次核实素材中确无该清单。
- V3 证明的是"每条内容都能在素材原文中找到出处"——**也为真**（8 条枚举句 + 4 条 K 卡正文）。
- V2 的过度保守之处是把"没有独立清单"升级成了"内容是归纳（可能含模型知识）"的自我怀疑措辞；V3 找到 21 句枚举 + K-17 挂钩 R21 的锚点后，这个怀疑被解除。

---

## 二、V-02：40 → 22 逐条去向

### 2.1 结论先行

1. **V2 的"约 40 条"是估数，且高估**。对 V2 提交快照（`5acc1b5:src/data/codes.json`）用与 V3 相同口径（name 含 未展开/未单独/未逐条/未定义/未披露/未提供）实测：**24 条**（纯家族 22 + C3、ISSUE-082 两个非家族条目）；放宽到含"推断/仅引用/演练复盘"措辞也只有 **27 条**。报告写作时未留清单，40 是向上估计。
2. **18 条消失是伪问题**。V2 快照的 22 条纯家族编号与当前 codes.json 的 22 条**集合完全一致，逐条可验证，0 条消失、0 条合并、0 条删除**（见 2.2 表）。
3. V2→V3 的真实变化发生在家族清单之外的 3 条错误命名（M35/M27/M21，V2 名称取错 K 卡标题），以及无锚点 M 条目注入主题池。

### 2.2 22 条家族命名逐条现状（V2 快照 vs 当前 codes.json 实测）

| 编号 | V2 名称 | 当前状态 | 当前名称（V3） |
|---|---|---|---|
| G1 | 上线硬门槛 G1（…素材未逐条展开） | 保留标注（**V4 发现素材有定义，见 V-06**） | 同 V2 |
| G2 | 同上格式 | 保留标注 | 同 V2 |
| G3 | 同上 | 保留标注 | 同 V2 |
| G5 | 同上 | 保留标注 | 同 V2 |
| G6 | 同上 | 保留标注 | 同 V2 |
| G7 | 同上 | 保留标注 | 同 V2 |
| G8 | 同上 | 保留标注 | 同 V2 |
| G10 | 同上 | 保留标注（素材仅"G1–G10"整体引用） | 同 V2 |
| M3 | 管理/交付线决策 M3（素材未单独展开） | 保留 + V3 注入 27 句主题池（fullContent 1161 字） | 同 V2 |
| M8 | 同上 | 保留 + 主题池（见 V-03，判定不通过） | 同 V2 |
| M9 | 同上 | 保留 + 主题池 | 同 V2 |
| M10 | 同上 | 保留 + 主题池 | 同 V2 |
| M13 | 同上 | 保留 + 主题池 | 同 V2 |
| M14 | 同上 | 保留 + 主题池 | 同 V2 |
| M16 | 同上 | 保留 + 主题池 | 同 V2 |
| M17 | 同上 | 保留 + 主题池 | 同 V2 |
| M18 | 同上 | 保留 + 主题池 | 同 V2 |
| M19 | 管理/交付线决策 M19（素材未单独展开） | 保留，V3 名称扩充 | "…（M 线主题池含"越权类验收门槛写死 0/100%"句，与 K-19 主题同源，编号对应关系素材未提供）" |
| M20 | 同上格式 | 保留，V3 名称扩充 | "…（与 K-22 主题同源…）" |
| M33 | 管理/交付线决策 M33（素材未单独展开） | 保留 + 主题池 | 同 V2 |
| P-11 | 性能口径条目（素材未展开，仅编号引用） | 保留标注 | 同 V2 |
| R24 | 演练复盘引用编号 R24（非 RAG R1–R21 线，素材未展开） | 保留标注 | 同 V2 |

另：V2 同口径命中的 C3、ISSUE-082 是事实陈述型命名（非家族性空洞），V3 未计入 22，合理。

---

## 三、V-03：M8 展示逻辑自查

**M8 点开后的完整展示（codes.json 实测摘录）**：

> **它是什么**：管理/交付线决策 M8（素材未单独展开）（管理/交付线）。
> 【素材边界说明（V3 溯源）】素材的 M 线"核心"仅提供以下 27 个主题分句，未提供 M1–M35 的逐条编号对应……以下是完整主题池供参考……
> 1. 三方"不参与"边界写入合同……（共 27 句全文铺开，从"门禁五只有两档计提"到"上线节点必须晚于门禁计提与省级审核"）
> 【有明确引用锚点的 M 编号】M0/M2（K-25）、M4/M5/M6（K-23）……M35（K-13）

**自问自答**：
1. 用户点 M8 想看"M8 是什么"——**展示的是 M 线整体背景**。
2. 27 句主题池是 M 线全部 27 个主题，不是 M8 的内容。
3. 用户看完**说不出 M8 是什么**——反而可能误以为第 8 句"端到端多模态不得用于批改核心路径"就是 M8（恰好句号=8，正是位置对位式误读）。

**判定：不通过，F-03 触发。** 修正方向（待确认后执行）：fullContent 头部改为「M8 · 素材未定义（素材只提供编号引用，未提供内容）」，主题池收缩为**定位参考说明**（说明 M 线有 27 个主题句、编号对应关系未提供、给出有锚点编号清单），不再铺 27 句全文。

---

## 四、V-04：M27 修复的逻辑自洽说明

### 4.1 两处原句引用（FIX_REPORT_V3 实测）

- 反驳 2（L14）："用已知锚点验证（K-24 挂 M23=范围裁决，但第 23 句主题是'数据下钻'）证明分句顺序与编号不线性相关，**位置对位会系统性错位**。"
- F-02 审核栏（L34）："M27 为何不取 K-33'三步法'主题？→ **分句 19 与 K-19 标题逐字一致**，K-33 主题在分句池中无对应句，取一致性强者，双锚点均保留。"

### 4.2 为什么不矛盾（逐条）

1. **位置对位**指"第 N 句 = M N"的推导方式，已被 K-24/M23 反例证伪。
2. **M27 的编号来源不是"第 19 句"**，而是素材明写的挂钩标注：K-19 卡标题原文「越权类验收门槛写死 0 次 / 100%，不给百分比**【已挂钩 M27 / R29】**」（合并版:656）。编号 27 来自这个标注。
3. "分句 19 与 K-19 标题一致"的作用是确认**主题句内容**（哪一句描述了 M27 挂的主题），不使用"19=27"的位置关系——分句序号 19 与编号 27 数值不等，本身就是位置对位未被使用的直接证明。
4. 素材原文核对：M 线分句 19 = "越权类验收门槛写死 0/100%"（Prompt L71），K-19 标题 = "越权类验收门槛写死 0 次 / 100%，不给百分比"——逐字一致成立（含措辞展开）。

**判定：不矛盾，M27 的内容映射（越权类验收门槛写死 0/100%）应保留，不应撤回。**

### 4.3 但发现新的执行 bug（V4 新增，比原问题更实在）

**M27/M35/M21 的 name 字段没有随 V3 修正更新**。当前 codes.json 实测：

| 编号 | name（点开先看到） | summary/fullContent（V3 已修正） | 素材证据 |
|---|---|---|---|
| M27 | 说"不"的三步法：翻成甲方损失 + 给可批替代 + 识别等价（卡片标题挂钩） | 越权类验收门槛写死 0 / 100% | K-19 标题挂钩 M27（合并版:656）；K-33 亦挂钩（:977） |
| M35 | 日历倒推：把不可控变成"有日期 + 签字人 + 后果条款"（卡片标题挂钩） | 越权负样本 ≥50 条作为上线前置 | K-13 追问答"（M35/D35）" |
| M21 | 日历倒推：…（卡片标题挂钩） | 合同条款只增不改 | M 线分句 21 + K-25/K-31 锚点 |

`code-knowledge.mjs` 的 NAMES 已写新名（L118-120），但 `parse-codes.mjs` L276 的 `else if (hookFromTitle[code])` **缺 `!name` 守卫**（前两个分支有 `!name`，第三个没有），curated 名称被"卡片标题挂钩"名覆盖。后果：点开 M27，标题说"三步法"、正文说"越权门槛写死"，**名字与内容自相矛盾**——这正是 V3 自我反驳说"取一致性强者"要避免的展示。**F-04 改判：撤回说法不需要，需要修的是这个 name 覆盖 bug（一行改动）。**

---

## 五、V-05：K-29 素材原文对照

### 5.1 素材原文（`03_三体预演与新增知识点.md` L49-61，合并版 L924 同文逐字一致）

> ### K-29 判定器不得是模型自评【已挂钩 V7 / N21】★技术 【T0】
> **【判别】** AI-401 明确规定"置信度是模型自评不是正确性保证"，拒绝"高置信自动通过"（V7）。但同一份材料里，AI-401 又用 0.70 置信度作为"低置信字段不进入规则引擎"的门槛——**这是用被自己否定过的东西当独立判定器**。
> **【统一判据】** 一个判定点要成立，它必须**不依赖被测对象的自我报告**。模型置信度、模型自评、同一模型的二次输出，都不构成独立判定。
> **【面试用法】** 这条能同时解释 V7（拒绝自动通过）和指出材料内部张力——**答出"我们内部这里有张力，我倾向于把 0.70 这个阈值的人类抽检常态化"是加分答案。**
> **【一句话记忆钩子】** > **模型说自己有多大把握，不构成独立判定——自评不是验证，那是同一个模型的第二次自述。**

### 5.2 cards.json K-29 实测

answerMarkdown（483 含空白 / 457 去空白）= 判别段 + 统一判据段 + 面试用法段 + 记忆钩子段（**素材四段逐字全在**）+ 追加的"素材说明（V3 逐行核查）：本卡源段在素材中仅 398 字…逐行比对无删减" + 关联卡 K-10。questions = "判定器不得是模型自评"（= 素材标题去挂钩标注）。

### 5.3 "398 字"统计口径说明

实测三种口径：素材章节**含空白 397 字**（含标题行与末尾 `---`）、去空白 368 字、去标题去分隔线 333 字。**V3 的"398"即"源段含空白字符数"口径（397±1 为边界行差），不是 K-29 全文另一种截断**。卡片 483 - 素材 397 ≈ 86 字 = 追加的素材说明与关联卡，正文无一处删减。

**判定：素材原文确实 ≈398 字，"素材受限"判定成立，F-05 不触发。**（顺带记录一个小瑕疵：卡片里钩子段与 `---` 分隔线粘连为"第二次自述。**---"，属排版问题非内容问题，可在下轮顺手修。）

---

## 六、V-06：G 系列散落线索穷举

全文搜索方法：对 7 个素材 md 逐行匹配 `G-N` / `GN`（后跟数字者排除，避免 G1 误吞 G10）。完整命中清单见 `_verify-v4-output.txt`。

| G 编号 | 素材所有出现位置（文件:行 + 关键原句） | 可否拼出内容 |
|---|---|---|
| **G-1** | 03:107 / 合并版:982："它会让 **G-1「AI 结论 = 0」**这条否决项失守，且否决项不允许整改后复验"；Prompt:133（K-33 三步法同句）；另 T1-11/模块3:96/Prompt:74/合并版:666 的"G1–G10"整体口径 | **是**：G1 = 「AI 结论 = 0」（不给结论）这一票否决项。**当前 codes.json 仍标"素材未逐条展开"→ 漏拼，F-06 触发** |
| G-2 | 仅"G1–G10"整体口径（模块3:96 / 合并版:666 / Prompt:74 / T1-11 行） | 否（整体口径中"越权/注入/写操作=0、个体行=0、审计覆盖 100%、拒答正确率 100%"共 6 个值，无法分配到 G2/G3/G5~G8 各条） |
| G-3 | 同上 | 否 |
| **G-4** | 模块1:270 / 合并版:447："家长申诉入口 + SLA 追踪（D3 US-6 / **D26 G-4**）。极高档硬要求。" | **部分可拼**：G-4 与家长申诉 SLA 硬要求同语境。当前 G4 名称已注明"另在申诉 SLA 语境被引用为 D26 G-4"，但未升为内容。F-06 可选补 |
| G-5 | 仅整体口径 | 否 |
| G-6 | 仅整体口径 | 否 |
| G-7 | 仅整体口径 | 否 |
| G-8 | 仅整体口径 | 否 |
| **G9** | 模块1:26、:101、模块3:302、合并版:203、:278、:872："G9 门槛要求首批 ≥50 个指标**全部已审批**才开问答入口" | **是（已有）**：codes.json 已命名，无遗漏 |
| G-10 | 仅"G1–G10"整体口径 | 否 |

**V-06 判定**：V3 的"G1~G8/G10 素材仅整体口径"**对 G1 不成立**（有明确定义句，散落在 K-33 三步法的论证里）；对 G4 部分成立（语境引用）；对其余成立。**F-06 触发（G1 必修，G4 建议补一句语境说明）。**

---

## 七、按判定汇总的修复建议（等确认后执行）

| 修复 | 触发依据 | 动作 |
|---|---|---|
| F-01 | 不触发 | 仅把 V3 报告与 code-knowledge.mjs 中"7 枚举 + 5 正文"的数字改为 8+4 |
| F-02 | 改判 | 不需要"补去向"（0 条消失）；改修 `parse-codes.mjs` L276 加 `!name` 守卫，恢复 M27/M35/M21 的 curated 名称并重新生成 |
| F-03 | 触发 | M8 等无锚点 M 条目：fullContent 头部改为"素材未定义"，27 句主题池收缩为定位说明（不再全文铺开） |
| F-04 | 改判 | 与 F-02 合并：M27 逻辑保留，修 name/内容矛盾展示 |
| F-05 | 不触发 | 顺手修 K-29 卡"。**---"粘连排版 |
| F-06 | 触发 | G1 补定义「AI 结论 = 0」（03:107 / 合并版:982 原句）；G4 补申诉 SLA 语境说明 |
| 审计 V4 | 任务书新增三规则 | fullContent>800 且 name 含"未展开"报警；fullContent 含 ≥5 个其他编号报警；卡字数 < 源段 60% 报警——**用当前数据跑，规则 1 会抓到 M 系列 12 条（正对应 F-03）** |

## 八、证据工具与复跑方式

```bash
cd C:/Users/admin/WorkBuddy/interview-cards
git show 5acc1b5:src/data/codes.json > _codes-v2.json   # V2 快照
node scripts/_verify-v4.mjs    # 21 句枚举 / M 线 27 句 / M8/M27 条目 / K-29 对照 / G 系列命中
node scripts/_verify-v4b.mjs   # M 线分句编号核对 / V2 家族清单
```
