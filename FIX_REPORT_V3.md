# 修复报告 V3（内容溯源核查 + 遗留项收尾）

生成：2026-09-25

---

## 〇、双 Agent 声明（选项 B，明确无含糊）

**本项目采用单人自审模式，无独立第二审核方。** 当前环境的可用模型（GLM-5.3-Flash）就是执行者本身，不存在可调起的第二个独立模型——"执行 Agent 优先 GLM-5.3-Flash"在本环境中无法成立（没有其他模型 API 或 CLI 可用）。

作为选项 B 的补偿承诺，本轮在关键判断上执行了**显式自我反驳**，且两处反驳推翻了自己的初判（不是走过场）：

1. **C-01 初判"复核零命中 → 第 9 条是编造"被自我反驳推翻**：追问"为什么搜不到？"→ 发现 V2 检索用 `\b` 正则对中文无效（中文字符间无词边界），修正检索后 R13/R14 枚举原句都在。若未反驳，会错误删除两条有据内容。
2. **C-02 初判"按 M 线分句位置对位映射 M1~M35"被自我反驳推翻**：用已知锚点验证（K-24 挂 M23=范围裁决，但第 23 句主题是"数据下钻"）证明分句顺序与编号不线性相关，位置对位会系统性错位。若未反驳，会产生 27 条貌似有据实则错位的映射——比"家族命名"危害更大。
3. M35 的修正依据（K-13 原文锚点）是独立于分句池的硬证据，与反驳 2 的结论互洽。

---

## 一、核查结论（先核查后修复，核查证据见 TRACE_REPORT.md）

| 核查 | 结论 | 触发的修复 |
|---|---|---|
| C-01 R21 十二条 | **通过**（A 类 12/12，C 类 0） | F-01 不触发重构；仅修正卡片头声明 + 逐条标来源等级 |
| C-02 家族命名 22 条 | 通过 19 条；**发现 3 条锚点错配（真错误）**；M 线素材确无逐条编号表 | F-02：修正 M35/M27/M21；无锚点 M 条目注入主题池 |
| C-03 十张短卡 | **V2 判定成立**：10/10 逐行无漏读 | F-03 不触发；素材说明升级为含具体字数 |

---

## 二、修复执行记录

| 任务 | 动作 | 审核 |
|---|---|---|
| F-01 R21 | 更新 `code-knowledge.mjs`：头声明改为"素材以 21 句枚举提供定义"；12 条逐条标注来源等级（7 条枚举原句 A、5 条 K 卡正文 A′）；附溯源方法说明 | 复查：每条的素材片段在 TRACE_REPORT 表中可对账 ✅ |
| F-02 锚点错配 | 新增 curated 条目：M35（K-13 原文锚点）、M27（分句 19 + K-19/K-33 锚点）、M21（分句 21 + K-25/K-31 锚点）；M19/M20 名称注明"主题同源、对应未证实" | 自我反驳：M27 为何不取 K-33"三步法"主题？→ 分句 19 与 K-19 标题逐字一致，K-33 主题在分句池中无对应句，取一致性强者，双锚点均保留 ✅ |
| F-02 主题池 | `parse-codes.mjs` 新增 mLinePool：无锚点 M 条目 fullContent 注入 27 句主题池 + 诚实声明 + 有锚点编号清单 | 注入后这些条目点开可看到全部 27 个 M 主题（含"端到端多模态不得用于批改核心路径"等），不再是空洞标注 ✅ |
| F-03 短卡 | `parse-cards.mjs` 素材说明含具体源段字数 + "逐行比对无删减" | ✅ |
| F-04 audit V3 | 三条新规则 + V3 汇总输出 | **端到端自测**：注入污染条目（fullContent 含"通行实践"）→ audit 抓到 V3-cpollute-TEST-01 → 还原 → 归 0 ✅ |

## 三、新增 / 修改文件

- `scripts/code-knowledge.mjs` —— R21 声明修正；新增 M35/M27/M21 curated 内容；M19/M20 名称修正
- `scripts/parse-codes.mjs` —— mLinePool 主题池注入（无锚点 M 条目）
- `scripts/parse-cards.mjs` —— 短卡素材说明含字数
- `scripts/audit.mjs` —— V3 三规则（C 类污染 / 家族命名逃避 / 内容漏读）+ 汇总输出
- `scripts/_trace-c03.mjs` —— C-03 逐行比对脚本（核查证据工具，保留可复跑）
- `src/data/codes.json` / `src/data/cards.json` —— 重新生成
- `TRACE_REPORT.md` —— 三份核查证据

## 四、回归

- 卡片数 78（42 完整 + 32 T1 + 4 附录）✅
- audit.mjs：结构化问题 0，V3 三规则通过 ✅
- codes.json：333 条全有名称、0 未展开空洞标注（22 条原家族命名 → 19 条诚实标注 + 3 条修正映射，且无锚点 M 条目已带主题池实质内容）✅
- build 通过 ✅

## 五、剩余未解决问题（如实列出）

1. **M 线 12 条无锚点编号（M3/M8/M9/M10/M13/M14/M16~M18/M33 及部分）无法可靠映射**——这不是"放弃"，是素材事实：27 个主题分句与 M1–M35 编号无逐条对应关系。已注入主题池使其点开有内容。若需完全映射，唯一途径是回到 6 个 md 之外的底层材料（D 系列文档），本素材集内无解。
2. **G1~G8/G10 的逐条内容素材未披露**——素材只给整体口径。卡片诚实标注。
3. **P-11 / R24 素材零命中**——保留标注。
4. **N 线 21 句对 22 号**：素材并句导致 N22 无法从分句池定位（N22 仅在 K-18 挂钩出现，语义=评测相关），维持锚点式命名。

## 六、用户可实测路径

- 点 **R21** → 12 条逐条列出，每条带（A：原句/A′：正文）来源标注
- 点 **M35** → K-13 原文锚点"越权负样本 ≥50 条作为上线前置（M35/D35）"
- 点 **M8** → 27 句 M 线主题池 + 诚实声明（不再是空洞的"素材未展开"）
- 打开 **K-29** → 完整原文 + "本卡源段在素材中仅 398 字…逐行比对无删减"
