# 题库融合 · 流程可靠性方案（RELIABILITY_PLAN）

建立：2026-09-26
适用范围：把《薪多多.xlsx》技术知识点（18 模块 / 154 个二级目录）与《简历 v13》项目经验融合，产出并入现有站点的新技术题库。
本方案是**执行契约**，优先级高于任何提示词。提示词是执行件，本方案是判据。

---

## 〇、模型约束（2026-09-26 确认）

- 主会话（编排者）：**DeepSeek v4-pro（正式版）**——强模型
- 子会话（执行/审核）：DeepSeek v4.1-flash

主会话为强模型，恢复「强模型编排 + flash 执行」的可靠形态：

1. **P0 主会话亲做并冻结**：锚点表、冲突裁决、终验基线由主会话亲自构建，产物为 `plan/anchors.json` 与 `plan/baseline.json`。冻结后二者是唯一事实源，后续只读；确需修改须重走人工确认，不留"先改后补"。
2. **闸 2 脚本化（保留）**：数字与编号比对不靠模型判断，用 `plan/verify.mjs` 对 `anchors.json` / `baseline.json` 自动比对输出 PASS/FAIL；主会话只负责跑脚本、读结果——机械比对不进模型。
3. **判定权上收（保留）**：闸 3 中"锚点表本身错"的判定权不在 flash——任何一组触发系统性故障且怀疑锚点表有误，立即停止全部批次，输出问题清单交人工。
4. **升级规则（保留）**：事实冲突按已确认原则「一律以简历 v13 为准」由主会话裁决；但遇到「两个文档打架且超出已登记冲突清单 / 事实拿不准 / 闸门判定两可」→ 停止并输出问题清单给用户，禁止自行取舍。
5. **并发 = 3**：强编排者可稳定管理 3 个 flash 子会话。
6. 四视角审核仍由 flash 承担（互不知作者）；`blocker` 回填后**必须重跑 verify.mjs**，不得凭"看起来对"放行。

---

## 一、问题的本质：上一版的错误放大链

上一版题库"不够丰富 + 有逻辑漏洞"，根因不是提示词写得差，而是**阶段之间没有闸门，导致一次小缺失放大成整库污染**：

```
素材层缺（M3/M8/M9/M10/M13/M14/M16/M17/M18/M19/M20/M33 共 12 个编号在素材中出现 0 次）
   ↓ 子会话不报缺，改用推断填满
推断层污染（codes.json 333 条编号中仅 38 条人工核校，265 条为"自动聚合 + 上下文推断"）
   ↓ 推断产物被下游当作事实引用
关联层错乱（X 系列的 R/M 是"三体预演"轮次编号，与 K 卡的 RAG 线编号是两套体系，导致 X-01 错连向量库主题）
   ↓ 无人复核编号语义
审计层失效（AUDIT_REPORT_V2 只检查"字数够不够"，查不出语义错、查不出数字错）
```

证据文件：

- `AUDIT_REPORT_V2.md` §三、§五
- `M_LINE_PENDING.md`
- `rewrite-out/*-review-*.json`（上一轮四视角审核产物，机制有效可复用）

**结论：本方案的重点不是"把提示词写得更严"，而是在层与层之间加可判定的闸门。**

---

## 二、五道闸 + 一个回退机制

### 闸 1｜准入 / 准出准则（可判定，不靠感觉）

| 阶段 | 准入（不满足不进） | 准出（不满足不出） |
|---|---|---|
| **P0 锚点表** | 简历 v13、`src/data/codes.json`、`scripts/code-overrides.mjs`、`src/data/cards.json` 四份都读过 | ① 每个 code 有唯一释义 ② 同名不同义 100% 拆分为新前缀 ③ 冲突清单逐条标注裁决结论 ④ `GAP-*` 显式列出 ⑤ **人工确认冻结** |
| **P1 生成** | P0 已冻结；已按 `allowed_topics` 过滤出本组锚点子集 | ① JSON 可被 `JSON.parse` 直接解析 ② **造数字 = 0** ③ **造编号 = 0** ④ `gap` 标注与实际素材缺口匹配 ⑤ 卡片正文 350~600 字 |
| **P2 连线** | P1 全部通过闸 1 | `related` 双向对称（A→B 必有 B→A），无指向不存在 id 的连线 |
| **P3 审核** | P2 通过 | 所有 `blocker` 已回填并复检通过 |
| **P4 上线** | P3 通过 **且终验基线全绿** | 线上可打开，技术 tab 可见，SRS 队列权重符合预期 |

### 闸 2｜抽样校验（必须放在四视角审核**之前**）

每组产出后，主会话先抽 **3 张卡**（1 张最长 + 1 张最短 + 1 张 `gap:true`），逐字比对锚点表：

| 检查 | 判定 |
|---|---|
| 卡内每个数字能否在锚点表逐字找到 | 找不到 → **立即打回全组重跑**，不等四视角 |
| 卡内每个 code 是否都在锚点表内 | 不在 → 同上 |
| 3 张中出现 ≥1 个 blocker | 判定为"系统性故障"，**整组重派**，不逐卡修 |

理由：造数字/造编号是**系统性缺陷**（同一份 flash 提示词导致的），不是随机缺陷。抽 3 张即可探到，逐卡审是浪费。

### 闸 3｜回退策略（分级，禁止就地打补丁）

| 故障范围 | 处置 | 重派时提示词必须改什么 |
|---|---|---|
| 单卡问题（1~2 张） | 卡级补：只重派这 2 张 | 无需改动 |
| 整组共性错（≥3 张同类） | **整组重派**，不复用旧产出 | 在"铁律"中追加针对性反例（例：`禁止写"6 段""7 段"这类段落数，段落数必须引用 NUM-xxx`）；并把该组素材切片进一步裁小 |
| 锚点表本身错 | **停止所有 P1，回退到 P0** | 修锚点表 → 重新冻结 → **所有已完成组全部重跑作废** |

第三条是关键：**锚点表是唯一事实源，它一错，下游全部作废。**

### 闸 4｜终验基线（回归测试，自动比对）

固化 10~15 条**已知正确答案的硬事实**于 `baseline.json`，合并完成后自动跑一遍，任一不符即拦下。

### 闸 5｜分阶段上线（禁止一次性铺开）

```
tech1 单组试跑（26 张）
  → 人工逐张审
  → 通过：确认「锚点表 + 提示词模板」契约有效 → 再并行铺 tech2~tech5
  → 不通过：只改契约，不改内容，重跑 tech1
```

理由：契约有缺陷时，26 张的试错成本是 130 张的 1/5。**先证明流程对，再放大产量。**

---

## 三、锚点表规范（P0 产物：`anchors.json`）

### 3.1 唯一原则：一码一义

> 同一编号在全库只能有一个含义。

上一版的关联错乱，全部源于"同一个 R6 在 K 卡里指向量库双选、在 X 卡里指三体预演轮次"。凡发现同名不同义，**必须拆分为新前缀，并在锚点表登记一次**。

### 3.2 编号体系（单一体系，前缀即语义）

| 前缀 | 含义 | 来源 |
|---|---|---|
| `CV-*` | 简历个人事实（经历、职级、职责） | 简历 v13 |
| `PRJ-*` | 项目事实（两个主力项目 + 三条副线） | 简历 v13 |
| `NUM-*` | 可引用数字（含口径与出处） | 简历 v13 |
| `DEC-*` | 技术与架构决策（沿用素材原有 `ADR-xxx` 原编号，写进 `original_code`） | 简历 v13 + 素材 |
| `GOV-*` | 合规红线与验收门槛（越权召回 0 次、G1–G10、R21 十二禁项等） | 简历 v13 + 素材 |
| `GAP-*` | 已知素材缺口（含 `M_LINE_PENDING.md` 中 12 个锁定不到的编号） | 素材 |

旧资产中的 `K-*` / `X-*` / `T1-*` / `M-*` / `R-*` / `V-*` / `N-*` / `AI-*` / `G*` / `ISSUE-*` / `AC-*` / `T-*` / `C*` / `APP-*` 编号 **一律保留原样**，但必须在锚点表内统一定义一次，写入 `original_code` 字段以便溯源。

### 3.3 字段契约

```json
{
  "code": "NUM-007",
  "type": "cv | project | number | decision | compliance | gap",
  "original_code": "M12",
  "where": "简历 v13 / 项目经历 / 浙江省智慧教育平台 AI 能力建设 / 牵头交付段",
  "text": "原文逐字摘录，不得改写",
  "quotable_numbers": ["229 张", "158 条"],
  "allowed_topics": ["项目管理", "交付治理"],
  "note": "适用范围与边界"
}
```

### 3.4 冲突裁决原则（已确认）

> **一律以《简历 v13 final》为唯一事实源。** v3、v10~v12、`BRIEF.md`、`resume_current*.txt` 中的旧口径全部作废，并在锚点表内登记为 `deprecated`。

已识别的三处冲突（P0 必须落表）：

| 事实 | 旧口径 | v13 口径（**采用**） | 旧口径出处 |
|---|---|---|---|
| P95 时延分段 | 7 段 | **6 段** | `resume_current_v3.txt` |
| 产业链平台二期截止 | 2025.03 | **2023.03** | `resume_current_v3.txt` |
| 数据表规模 | 290 表 / 2,175 列 | **229 张**（编号零断点） | `rewrite-out/BRIEF.md` |

---

## 四、技术卡产出契约（P1 用）

```json
{
  "id": "TT1-001",
  "chapter": "tech1",
  "title": "二级目录原文",
  "questions": ["面试官会怎么问 3~5 个问句，首项 = title"],
  "answerMarkdown": "## 结论\n…\n## 原理与边界\n…\n## 生产实践\n…（必须显式引用 ≥1 个锚点 code）",
  "memoryHook": "一句话钩子，30 字内",
  "tags": ["技术"],
  "anchors": ["NUM-007", "DEC-004"],
  "gap": false,
  "gapNote": "",
  "priority": "T0 | T1",
  "related": ["TT1-003"],
  "srs": true
}
```

硬约束：

1. 三段式段标题固定为 `## 结论` / `## 原理与边界` / `## 生产实践`，不得改名。
2. 卡片正文 350~600 字；超过 800 字说明未合并三级子点。
3. 「生产实践」段必须至少一处落到两个省级项目上。
4. `related` 只连本组内；跨组连线交由 tech6 组处理。

---

## 五、分组与产量

实测《知识点梳理》sheet 共 **154 个二级目录**（python 9 / linux 9 / redis 9 / MySQL 10 / fastapi 9 / langchain 9 / langgraph 9 / RAG 9 / agent 11 / 知识图谱 9 / git 9 / Dify 12 / Docker 11 / nginx 6 / http协议 8 / 流程图 5 / 甘特图 5 / 禅道 5）。

切分规则：**一个二级目录 = 一张卡**，其下三级子点合并进同一张卡（作为追问线索）。

| 组 | 覆盖一级模块 | 预计卡数 | 状态 |
|---|---|---|---|
| tech1 编程基础 | python、http协议、fastapi | 26 | **先试跑** |
| tech2 数据缓存 | MySQL、redis | 19 | 待铺开 |
| tech3 大模型应用 | langchain、langgraph、RAG、agent、知识图谱、Dify | 59 | 待铺开 |
| tech4 部署运维 | linux、Docker、nginx、git | 35 | 待铺开 |
| tech5 工程交付 | 流程图、甘特图、禅道 | 15 | 待铺开 |
| tech6 交叉连线 | 不产新卡，只做连线与缺口登记 | 0 | 待铺开 |

并发约束：同一时间最多 **3** 个子会话（强编排主会话，见 §〇）。

---

## 六、执行顺序与人工介入点

| # | 动作 | 执行者 | 人工介入 |
|---|---|---|---|
| 0 | 建锚点表 + 基线 + 校验脚本（P0）并冻结 | 主会话（v4-pro 亲做） | 逐条裁决冲突并确认冻结 |
| 1 | 读冻结产物，作为后续唯一事实源 | 主会话（对 anchors/baseline 只读） | — |
| 2 | tech1 试跑 26 张 | flash 子会话 | — |
| 3 | 跑 verify.mjs + 四视角审核 | 主会话 + flash | **人工逐张审 26 张** |
| 4 | 铺开 tech2~tech5 | flash（并发 ≤3） | — |
| 5 | 终验基线比对 | verify.mjs（自动） | — |
| 6 | 合并 → `npm run parse` → 上线 | 主会话 | 线上验收 |

新增技术 tab 需要在 `CHAPTERS`（见 `src/lib/cards.js`）中加 `tech1`~`tech6` 六个章节，并确定新卡的 SRS 权重（建议：技术卡 `srs: true` 但排在项目卡之后）。

---

## 七、四视角交叉审核（P3，复用上一轮机制）

四个子会话**逐个交叉、互不知作者**，只读不改：

| 视角 | 只问这一件事 |
|---|---|
| `interview` 面试官 | 这题问出来，答不上来会卡在哪？追问链断没断？ |
| `arch` 架构师 | 技术结论有没有硬错？三段式里的因果关系对不对？ |
| `gov` 合规 | 有没有造数字、造编号、把红线说轻？ |
| `pm` 交付 | 生产实践段是否真能落到项目上，还是空话？ |

输出契约：

```json
[{ "id": "TT1-001", "level": "blocker|major|minor", "problem": "", "evidence": "", "fix": "" }]
```

回填规则：主会话汇总去重后只回填 `blocker` / `major`；**回填时另开子会话，只给问题清单 + 原卡，不给审核者身份**，避免其顺着审核者的话编。

---

## 八、禁令

1. 禁止直接修改 `src/data/cards.json`——必须走 `npm run parse` 管线，否则破坏 Gist 中按 `updatedAt` 合并的 SRS 进度。
2. 禁止 `git push --force`。
3. 禁止触碰 Gist 中的进度数据。
4. 禁止在卡片正文里写审核痕迹（"素材未展开""以下为上下文推断""关联卡暂无""出处：某文件"）。不足就标 `gap`，由人工裁决，不在正文解释。
5. 禁止编造素材中没有的具体数字。需要量化时用锚点表已有数字，没有就写定性描述。
6. 禁止伪造项目事实（编造素材中没有的决策、事故、客户名）。

---

## 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-09-26 | 建立本方案 | 上一版缺阶段门禁与回退机制，错误逐层放大 |